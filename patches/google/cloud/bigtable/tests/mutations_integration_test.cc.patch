diff --git a/google/cloud/bigtable/tests/mutations_integration_test.cc b/google/cloud/bigtable/tests/mutations_integration_test.cc
index b194c10..d1d08af 100644
--- a/google/cloud/bigtable/tests/mutations_integration_test.cc
+++ b/google/cloud/bigtable/tests/mutations_integration_test.cc
@@ -43,9 +43,9 @@ class MutationIntegrationTest : public bigtable::testing::TableIntegrationTest {
    */
   void CreateCellsIgnoringTimestamp(bigtable::Table& table,
                                     std::vector<bigtable::Cell> const& cells) {
-    std::map<std::string, bigtable::SingleRowMutation> mutations;
+    std::map<absl::Cord, bigtable::SingleRowMutation> mutations;
     for (auto const& cell : cells) {
-      std::string key = cell.row_key();
+      auto key = cell.row_key();
       auto inserted = mutations.emplace(key, bigtable::SingleRowMutation(key));
       inserted.first->second.emplace_back(bigtable::SetCell(
           cell.family_name(), cell.column_qualifier(), cell.value()));
